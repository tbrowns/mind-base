import { LoaderCircle } from "@/components/icons";
export function PageHeader({ eyebrow, title, description, action }: { eyebrow?: string; title: string; description: string; action?: React.ReactNode }) { return <div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div>{eyebrow && <p className="mb-2 text-[11px] font-bold uppercase tracking-[.18em] text-[#21a67a]">{eyebrow}</p>}<h1 className="text-3xl font-semibold tracking-[-.035em] text-[#17211c] md:text-[38px]">{title}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[#6b756e]">{description}</p></div>{action}</div>; }
export function Spinner({ size = 18 }: { size?: number }) { return <LoaderCircle size={size} className="animate-spin" />; }
export function Badge({ children, tone = "green" }: { children: React.ReactNode; tone?: "green" | "gold" | "gray" }) { const classes = tone === "green" ? "bg-[#e8f5ef] text-[#197a5b]" : tone === "gold" ? "bg-[#fff5d9] text-[#8a6511]" : "bg-[#f0f2ef] text-[#657068]"; return <span className={`inline-flex rounded-full px-2.5 py-1 text-[10px] font-bold uppercase tracking-[.08em] ${classes}`}>{children}</span>; }
